<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Editar</title>
    <style>
        body{
            margin: 0; padding: 0; background: #ddd;
        }

        .container-edit{
            width: 100%;
            display: flex;
            justify-content: center;
        }

        .edit{
            width: 30%;
            padding: 10px;
            background: #fff;
            box-shadow: 0px 0px 2px grey;
            text-align: center;
            margin-top: 30px;
        }

        .edit input[type="text"]{
            width: 80%;
            padding: 5px;
        }

        .edit textarea{
            width: 80%;
            overflow: unset;
        }

        .edit button{
            padding: 10px 20px;
            border: none;
            background: #01c901;
            color: #fff;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container-edit">
        <form action="/editar/<%= editar.id %>" method="POST" class="edit" autocomplete="off">
            <label>Titulo:</label><br><br>
            <input type="text" name="titulo" value="<%= editar.titulo %>">
            <br><br>

            <Label>Descripción</Label><br><br>
            <textarea name="descripcion" rows="10"><%= editar.descripcion %></textarea>
                <br><br>
            <button type="submit">
                Editar
            </button>
        </form>
    </div>
</body>
</html>